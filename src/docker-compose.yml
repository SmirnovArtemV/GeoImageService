services:
  postgres:
    image: postgres:latest
    container_name: geo-image-postgres
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: geo_image_db
    ports:
      - "5432:5432"
    restart: unless-stopped